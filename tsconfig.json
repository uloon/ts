{
/*
    ts编译器配置文件
*/
    
    //指定需要被编译的ts文件
    "include": [
        "./**/*"
    ],
    //排除不需要编译的文件
    "exclude": [

    ],
    //定义被继承的配置文件
    // "extends":"要继承的文件路径"

    //编译器选项
    "compilerOptions": {
        "target": "ES6",//指定编译后的es版本
        "module": "ES6",//指定模块化规范
        // "lib": []//指定项目中的库，一般不用
        // "outDir": "./01"//指定编译后的js文件目录
        // "outFile": "./01/app.js"//，合并成一个文件，仅支持amd和system模块化
        "allowJs": false,//是否对js文件进行编译
        "checkJs": false,//是否检查js代码符合语法规范
        "removeComments": false,//是否移除注释
        // "noEmit": false,//不生成编译后的文件
        "noEmitOnError": false,//有错误时不生成编译文件
        // "alwaysStrict": false,//编译后的文件是否使用严格模式
        // "noImplicitAny": false,//隐式的any类型
    }
}